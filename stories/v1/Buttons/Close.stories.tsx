import React from "react";

import ButtonClose from "../../../components/v1/Buttons/Close";

export default {
    component: ButtonClose,
    title: "Risedle V1/Buttons/Close",
};

export const Close = () => <ButtonClose onClick={() => {}} />;
